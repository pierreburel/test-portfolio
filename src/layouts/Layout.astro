---
import { getLayoutProps, type LayoutProps } from '~/utils'

type Props = LayoutProps<{ title?: string }>

const { title = 'Portfolio' } = getLayoutProps(Astro.props)
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <a href="/test-portfolio/">Home</a>
    <slot />
  </body>
</html>
